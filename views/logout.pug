extends layout

block content
  h1 Logout

  form(action="/logout" method="POST")
    .form-field
      p You are logged in as #{username}

    .form-field
      button.submit-button(type="submit") logout

  a(href="/") index
